{
    "auto_engine": [
        "Open {browser} and search {*query}",
        "On {browser} search {*query}",
        "with {browser} search/open {*query}",
        "open/search/play {*query} on/with {browser}"
    ],
    "add_template_to": [
        "Append/Link {*template_to_add} to {method_to_add}"
    ],
    "print_templates": [
        "what can i say",
        "what can i say ?"
    ],
    "remove_template_from": [
        "remove {*template_to_remove} from {linked_method}"
    ],
    "add_plugin": [
        "add plugin {plugin_clone_url}"
    ],
    "show_plugins": [
        "Show me/ the available/ plugins"
    ],
    "remove_plugin": [
        "remove the/ plugin named/ {plugin_name}"
    ],
        "add_plugin_from_memory": [
        "Add the/ local plugin {plugin_path}"
    ],
    "disable_plugin": [
        "Disable the/ plugin named/ {plugin_name}"
    ]
}